Export `t2v_api.json` and `i2v_api.json` from your own ComfyUI instance into
this folder — see step 3 in the top-level README.md. Nothing here is
pre-built; a guessed workflow file would silently fail or produce garbage,
so this is intentionally left for your one-time setup.
